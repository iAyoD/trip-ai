# TripAI - 智能旅行规划助手

TripAI 是一个基于人工智能的旅行规划应用，旨在为您生成个性化的完美行程。告别繁琐的攻略制作，只需几秒钟，AI 就能为您量身定制包含景点、美食、酒店的详细行程方案。

## ✨ 功能特点

- **🤖 智能路线规划**：利用 LLM 强大的语言模型，根据您的目的地、时间、预算和偏好，自动生成最优游览路线。
- **🗺️ 可视化地图展示**：结合地图数据，直观展示每日行程路线（前端集成高德地图/Mapbox等）。
- **💰 精准预算控制**：根据您的预算范围（经济/中等/豪华），推荐合适的住宿和餐饮，并预估总费用。
- **🎨 完全个性化定制**：支持多种旅行偏好（历史文化、自然风光、购物娱乐等），打造独一无二的体验。
- **🖼️ 景点美图展示**：自动集成 Unsplash API，为每个推荐景点匹配精美图片。
- **📄 行程导出**：支持将生成的行程方案导出为 PDF，方便离线查看。

## 🛠️ 技术栈

### 前端 (Frontend)
- **框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **UI 组件库**: Ant Design Vue
- **样式**: Tailwind CSS
- **状态管理**: Pinia
- **路由**: Vue Router
- **HTTP 客户端**: Axios
- **图标**: Lucide Vue Next
- **工具库**: Day.js, html2canvas, jspdf

### 后端 (Backend)
- **框架**: FastAPI (Python)
- **AI 模型**: 支持 OpenAI SDK 的 API
- **图片服务**: Unsplash API
- **数据验证**: Pydantic
- **服务容器**: Uvicorn

## 🚀 快速开始

### 前置要求
- Node.js >= 16
- Python >= 3.8
- LLM API Key
- Unsplash Access Key
- 高德地图 API Key

### 1. 克隆项目
```bash
git clone <repository-url>
cd trip-planner
```

### 2. 后端设置
```bash
cd backend

# 创建虚拟环境 (推荐)
uv venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入您的 API Keys:
# OPENAI_API_KEY=sk-...
# UNSPLASH_ACCESS_KEY=...
# AMAP_API_KEY=...
# 启动后端服务
uvicorn app.api.main:app --reload
```
后端 API 服务将在 `http://localhost:8000` 启动。

### 3. 前端设置
```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```
前端页面将在 `http://localhost:5173` (默认) 启动。

## 📝 环境变量说明

请确保在 `backend/.env` 文件中正确配置以下变量：

| 变量名 | 说明 |
|--------|------|
| `OPENAI_API_KEY` | LLM API 密钥，用于生成行程内容 |
| `UNSPLASH_ACCESS_KEY` | Unsplash API Access Key，用于获取景点图片 |
| `AMAP_API_KEY` | 高德地图 API Key，用于获取地理信息 |

## 🚀 部署指南

### 前端部署 (GitHub Pages)

本项目已配置 GitHub Actions 自动部署前端到 GitHub Pages。

1. **设置 Secrets**:
   在 GitHub 仓库的 `Settings` -> `Secrets and variables` -> `Actions` 中添加以下 Repository secret:
   - `VITE_API_BASE_URL`: 您的后端 API 地址 (例如: `https://your-backend-api.com/api`)

2. **启用 GitHub Pages**:
   在 GitHub 仓库的 `Settings` -> `Pages` 中，Build and deployment source 选择 `GitHub Actions`。

3. **自动部署**:
   推送到 `main` 分支时，GitHub Actions 将自动构建并部署前端。

## 📌 TODO

- [ ] 添加其他旅游平台 MCP（如小红书）

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request 来改进这个项目！

## 📄 许可证

MIT License
